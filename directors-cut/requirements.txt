# Director's Cut - HuggingFace Space Frontend
# Lightweight frontend that calls Modal backend for heavy processing

# Gradio 6 with MCP support
gradio[mcp]>=6.0.0

# HTTP client for Modal API calls
requests>=2.31.0
httpx>=0.25.0

# Utilities
python-dotenv>=1.0.0
